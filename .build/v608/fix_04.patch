+        ttk.Label(form,text='Описание фотографии',font=('Arial',int(app.settings.get('font_size',11)),'bold')).grid(row=row,column=0,columnspan=2,sticky='w',pady=(4,0));row+=1
+        desc_frame=ttk.Frame(form); desc_frame.grid(row=row,column=0,columnspan=2,sticky='nsew',pady=(4,8))
+        self.desc=tk.Text(desc_frame,height=5,wrap='word',font=('Arial',int(app.settings.get('font_size',11))),padx=10,pady=8,spacing1=3,spacing3=4,borderwidth=1,relief='solid'); desc_scroll=ttk.Scrollbar(desc_frame,orient='vertical',command=self.desc.yview); self.desc.configure(yscrollcommand=desc_scroll.set)
+        self.desc.pack(side='left',fill='both',expand=True); desc_scroll.pack(side='right',fill='y');desc_row=row;row+=1
+        pf=ttk.LabelFrame(form,text='Фотография',padding=6);pf.grid(row=row,column=0,columnspan=2,sticky='nsew');self.preview=ttk.Label(pf,text='Фото не выбрано',anchor='center');self.preview.pack(fill='both',expand=True)
+        pb=ttk.Frame(pf);pb.pack(fill='x',pady=5);ttk.Button(pb,text='Выбрать/заменить фото',command=self.choose_photo).pack(side='left');ttk.Button(pb,text='Удалить фото',command=self.remove_photo).pack(side='left',padx=5);ttk.Button(pb,text='Открыть',command=lambda:self.app.popup_photo(self.current_path)).pack(side='left');photo_row=row;row+=1
+        act=ttk.Frame(form);act.grid(row=row,column=0,columnspan=2,sticky='ew',pady=6);ttk.Button(act,text='Сохранить',command=self.save).pack(side='left');ttk.Button(act,text='Удалить карточку',command=self.delete).pack(side='left',padx=6);ttk.Button(act,text='Отмена/очистить',command=self.new).pack(side='left')
+        form.columnconfigure(0,weight=1);form.columnconfigure(1,weight=1);form.rowconfigure(desc_row,weight=1);form.rowconfigure(photo_row,weight=1)
         for e in self.entries.values():
             e.bind('<KeyRelease>',self.schedule_autosave); e.bind('<FocusOut>',self.schedule_autosave)
             e.bind('<Control-c>',lambda ev:self._edit_event(ev,'<<Copy>>')); e.bind('<Control-C>',lambda ev:self._edit_event(ev,'<<Copy>>'))
@@ -1564,6 +1592,36 @@
             try:widget.bind('<Button-3>',self.show_context_menu)
             except Exception:pass
         self.status()
+
+    def add_rows(self):
+        count=simpledialog.askinteger(
+            'Добавить строки',
+            'Сколько пустых строк добавить?',
+            parent=self, minvalue=1, maxvalue=10000, initialvalue=10
+        )
+        if not count:
+            return
+        ids=[]
+        try:
+            self.db.conn.execute('BEGIN')
+            for _ in range(int(count)):
+                ids.append(self.db.add({
+                    'archive_no':'','description':'','shot_date':'','location':'',
+                    'author':'','source':'','file_path':''
+                },commit=False))
+            self.db.conn.commit()
+        except Exception as exc:
+            self.db.conn.rollback()
+            messagebox.showerror(APP_NAME,f'Не удалось добавить строки:\n{exc}')
+            return
+        before={int(pid):None for pid in ids}; after=self.db.snapshot_states(ids)
+        self.app.record_history('Добавление пустых строк',before,after)
+        self.q.set('')
+        self.refresh()
+        first=str(ids[0]) if ids else ''
+        if first and self.tree.exists(first):
+            self.tree.selection_set(first); self.tree.focus(first); self.tree.see(first); self.select()
+        self.status_var.set(f'Добавлено пустых строк: {len(ids)}. Ctrl+Z — отменить.')
 
     def _make_context_menu(self):
         self.context_menu=tk.Menu(self,tearoff=0)
@@ -2337,12 +2395,12 @@
         self.q=tk.StringVar()
         ttk.Label(top1,text='Поиск:').pack(side='left')
         e=ttk.Entry(top1,textvariable=self.q,width=40);e.pack(side='left',padx=6);e.bind('<Return>',lambda x:self.new_search())
-        ttk.Button(top1,text='Найти',style='Primary.TButton',command=self.new_search).pack(side='left')
-        ttk.Button(top1,text='+ Добавить строки',style='Soft.TButton',command=self.add_rows).pack(side='left',padx=(8,0))
-        ttk.Button(top1,text='Отменить',style='Undo.TButton',command=self.app.undo_action).pack(side='left',padx=(8,0))
-        ttk.Button(top1,text='Вернуть',style='Redo.TButton',command=self.app.redo_action).pack(side='left',padx=4)
-        ttk.Button(top1,text='Найти и заменить…',style='Soft.TButton',command=self.open_find_replace).pack(side='left',padx=(4,0))
-        ttk.Label(top1,text='Двойной клик — редактировать • Ctrl+V — вставить таблицу • Правый клик — действия',style='Hint.TLabel').pack(side='right')
+        ttk.Button(top1,text='Найти',style='CompactPrimary.TButton',command=self.new_search).pack(side='left')
