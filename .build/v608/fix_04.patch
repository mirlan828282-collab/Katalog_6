@@ -2374,8 +2397,8 @@
         e=ttk.Entry(top1,textvariable=self.q,width=40);e.pack(side='left',padx=6);e.bind('<Return>',lambda x:self.new_search())
         ttk.Button(top1,text='Найти',style='CompactPrimary.TButton',command=self.new_search).pack(side='left')
         ttk.Button(top1,text='Добавить строки',style='Compact.TButton',command=self.add_rows).pack(side='left',padx=(5,0))
-        ttk.Button(top1,text='Отменить',style='Undo.TButton',command=self.app.undo_action).pack(side='left',padx=(5,0))
-        ttk.Button(top1,text='Вернуть',style='Redo.TButton',command=self.app.redo_action).pack(side='left',padx=3)
+        ttk.Button(top1,text='↶',style='Undo.TButton',width=3,command=self.app.undo_action).pack(side='left',padx=(5,1))
+        ttk.Button(top1,text='↷',style='Redo.TButton',width=3,command=self.app.redo_action).pack(side='left',padx=1)
         ttk.Button(top1,text='Найти / заменить',style='Compact.TButton',command=self.open_find_replace).pack(side='left',padx=(3,0))
         ttk.Label(top1,text='Двойной клик / F2 — редактировать • Ctrl+V — вставить • Правый клик — действия',style='Hint.TLabel').pack(side='right')
 
@@ -2403,7 +2426,7 @@
         self.tree.configure(yscrollcommand=self._tree_yscroll,xscrollcommand=self._tree_xscroll)
         self.tree.grid(row=0,column=0,sticky='nsew');ys.grid(row=0,column=1,sticky='ns');xs.grid(row=1,column=0,sticky='ew')
         fr.rowconfigure(0,weight=1);fr.columnconfigure(0,weight=1)
-        self.tree.bind('<Double-1>',self.on_double_click)
+        self.tree.bind('<Double-Button-1>',self.on_double_click)
         self.tree.bind('<Button-1>',self.cell_select_click,add='+')
         self.tree.bind('<ButtonRelease-1>',self.click_open)
         self.tree.bind('<<TreeviewSelect>>',self.show_selected_description)
@@ -2419,12 +2442,23 @@
         self.tree.bind('<Configure>',lambda e:self._schedule_overlays(),add='+')
         self._cell_editor=None
 
-        desc_box=ttk.LabelFrame(catalog_body,text='Полное описание выбранной записи',padding=7,style='Card.TLabelframe')
+        desc_box=ttk.LabelFrame(catalog_body,text='Полное описание выбранной записи',padding=8,style='Card.TLabelframe')
         catalog_body.add(desc_box,weight=1)
-        self.description_text=tk.Text(desc_box,height=6,wrap='word',state='disabled',font=('Arial',11),spacing1=4,spacing3=6,padx=12,pady=10,borderwidth=1,relief='solid')
-        desc_scroll=ttk.Scrollbar(desc_box,orient='vertical',command=self.description_text.yview)
+        ttk.Label(desc_box,text='Описание',font=('Arial',int(app.settings.get('font_size',11)),'bold')).grid(row=0,column=0,columnspan=2,sticky='w',pady=(0,4))
+        desc_frame=ttk.Frame(desc_box);desc_frame.grid(row=1,column=0,columnspan=2,sticky='nsew',pady=(0,7))
+        self.description_text=tk.Text(desc_frame,height=4,wrap='word',state='disabled',font=('Arial',11),spacing1=4,spacing3=6,padx=12,pady=9,borderwidth=1,relief='solid')
+        desc_scroll=ttk.Scrollbar(desc_frame,orient='vertical',command=self.description_text.yview)
         self.description_text.configure(yscrollcommand=desc_scroll.set)
         self.description_text.pack(side='left',fill='both',expand=True); desc_scroll.pack(side='right',fill='y')
+        self.catalog_meta_vars={k:tk.StringVar(value='') for k in ('archive_no','shot_date','location','author','source')}
+        meta=ttk.Frame(desc_box,padding=(2,1,2,0));meta.grid(row=2,column=0,columnspan=2,sticky='ew')
+        meta.columnconfigure(0,weight=1);meta.columnconfigure(1,weight=1)
+        ttk.Label(meta,textvariable=self.catalog_meta_vars['archive_no']).grid(row=0,column=0,sticky='w',padx=(0,14),pady=2)
+        ttk.Label(meta,textvariable=self.catalog_meta_vars['shot_date']).grid(row=0,column=1,sticky='w',pady=2)
+        ttk.Label(meta,textvariable=self.catalog_meta_vars['location']).grid(row=1,column=0,sticky='w',padx=(0,14),pady=2)
+        ttk.Label(meta,textvariable=self.catalog_meta_vars['author']).grid(row=1,column=1,sticky='w',pady=2)
+        ttk.Label(meta,textvariable=self.catalog_meta_vars['source']).grid(row=2,column=0,columnspan=2,sticky='w',pady=(2,0))
+        desc_box.columnconfigure(0,weight=1);desc_box.columnconfigure(1,weight=1);desc_box.rowconfigure(1,weight=1)
         self._make_context_menu()
         self.status()
 
@@ -2682,7 +2716,9 @@
         if field:
             self.active_cell=(str(iid),col); self.cell_anchor=self.active_cell; self.cell_selection={self.active_cell}
             self.tree.selection_set(iid); self.tree.focus(iid); self._draw_cell_selection()
-            self._begin_cell_edit(iid,col,field)
+            # Open after Tk finishes the double-click selection sequence. This keeps
+            # direct cell editing reliable even with custom Ctrl/Shift cell overlays.
+            self.after_idle(lambda ii=str(iid),cc=col,ff=field:self._begin_cell_edit(ii,cc,ff))
             return 'break'
 
     def edit_active_cell(self,event=None):
@@ -2821,21 +2857,26 @@
 
     def show_selected_description(self,_event=None):
         sel=self.tree.selection()
-        text=''
+        desc=''
