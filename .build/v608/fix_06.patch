+            self.tree.selection_set(iid); self.tree.focus(iid); self._draw_cell_selection()
+            # Open after Tk finishes the double-click selection sequence. This keeps
+            # direct cell editing reliable even with custom Ctrl/Shift cell overlays.
+            self.after_idle(lambda ii=str(iid),cc=col,ff=field:self._begin_cell_edit(ii,cc,ff))
+            return 'break'
+
+    def edit_active_cell(self,event=None):
+        cell=self.active_cell or self.cell_anchor
+        if not cell:
+            iid=self.tree.focus()
+            if not iid:
+                sel=self.tree.selection(); iid=sel[0] if sel else ''
+            if iid:
+                cell=(str(iid),'#3')
+        if not cell:
+            return 'break'
+        iid,col=cell
+        field=self.COLUMN_FIELDS.get(col)
+        if iid and field and self.tree.exists(iid):
+            self.active_cell=(iid,col); self.cell_anchor=self.active_cell; self.cell_selection={self.active_cell}
+            self.tree.selection_set(iid); self.tree.focus(iid); self._draw_cell_selection()
             self._begin_cell_edit(iid,col,field)
-            return 'break'
+        return 'break'
 
     def _begin_cell_edit(self,iid,col,field):
         if self._cell_editor is not None:
@@ -2724,6 +2817,7 @@
         self.context_menu.add_command(label='Вернуть    Ctrl+Y',command=self.app.redo_action)
         self.context_menu.add_command(label='Найти и заменить в описаниях…',command=self.open_find_replace)
         self.context_menu.add_separator()
+        self.context_menu.add_command(label='Редактировать ячейку    F2',command=self.edit_active_cell)
         self.context_menu.add_command(label='Открыть выбранную фотографию',command=self.open_selected_photo)
         self.context_menu.add_command(label='Копировать ячейки/строки    Ctrl+C',command=self.copy_selected)
         self.context_menu.add_command(label='Вставить таблицу из Word / Excel    Ctrl+V',command=self.paste_table_into_catalog)
@@ -2763,21 +2857,26 @@
 
     def show_selected_description(self,_event=None):
         sel=self.tree.selection()
-        text=''
+        desc=''
+        values={'archive_no':'Архивный номер: —','shot_date':'Дата съёмки: —','location':'Место съёмки: —','author':'Автор: —','source':'Источник поступления: —'}
         if sel:
             r=self.db.get(int(sel[0]))
             if r:
-                text=(f"Архивный номер: {safe_text(r['archive_no'])}\n"
-                      f"Дата съёмки: {safe_text(r['shot_date'])}\n"
-                      f"Место съёмки: {safe_text(r['location'])}\n"
-                      f"Автор: {safe_text(r['author'])}\n"
-                      f"Источник поступления: {safe_text(r['source'])}\n\n"
-                      f"Описание:\n{safe_text(r['description'])}")
+                desc=safe_text(r['description'])
+                values={
+                    'archive_no':f"Архивный номер: {safe_text(r['archive_no'])}",
+                    'shot_date':f"Дата съёмки: {safe_text(r['shot_date'])}",
+                    'location':f"Место съёмки: {safe_text(r['location'])}",
+                    'author':f"Автор: {safe_text(r['author'])}",
+                    'source':f"Источник поступления: {safe_text(r['source'])}",
+                }
         self.description_text.configure(state='normal')
         self.description_text.delete('1.0','end')
-        self.description_text.insert('1.0',text)
+        self.description_text.insert('1.0',desc)
         self.description_text.configure(state='disabled')
         self.description_text.yview_moveto(0)
+        for key,var in getattr(self,'catalog_meta_vars',{}).items():
+            var.set(values.get(key,''))
 
     def new_search(self):
         self.cell_selection.clear(); self.cell_anchor=None; self.active_cell=None; self.refresh()
@@ -2887,7 +2986,10 @@
             x,y,w,h=box
             if w<=1 or h<=1:continue
             for px,py,pw,ph in ((x,y,w,2),(x,y+h-2,w,2),(x,y,2,h),(x+w-2,y,2,h)):
-                edge=tk.Frame(self.tree,bg=accent);edge.place(x=px,y=py,width=max(1,pw),height=max(1,ph));self._cell_overlays.append(edge)
+                edge=tk.Frame(self.tree,bg=accent,cursor='arrow',takefocus=0)
+                edge.place(x=px,y=py,width=max(1,pw),height=max(1,ph))
+                edge.bind('<Double-1>',lambda e,ii=iid,cc=col:self._begin_cell_edit(ii,cc,self.COLUMN_FIELDS[cc]))
+                self._cell_overlays.append(edge)
             shown+=1
             if shown>200:break
 
